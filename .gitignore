node_modules/
dist/
.env
.env.local
*.tgz

# Claude Code MCP config (contains user-specific JWT — fill in from .mcp.json.example)
.mcp.json

# Codex CLI MCP config (contains user-specific JWT — fill in from config.toml.example)
.codex/config.toml
